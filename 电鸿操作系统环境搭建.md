# 电鸿 (PowerHarmony) 环境搭建

对应 SDK：V2.0.0.0。环境：Ubuntu 22.04，x86_64，VMware。

## 1. 准备工作

- Ubuntu 18.04+（实测 22.04），内存推荐 ≥16G
- **用户名不能含中文字符**
- Windows 10 64位（用于烧录）

## 2. Samba 共享（Windows 访问 Ubuntu 文件）

```bash
sudo apt install -y samba samba-common
mkdir -p /home/zrx/share

# 追加配置到 /etc/samba/smb.conf（整块复制粘贴，勿逐行）
sudo tee -a /etc/samba/smb.conf > /dev/null <<'EOF'

[Share]
path = /home/zrx/share
valid users = zrx
directory mask = 0775
create mask = 0775
writable = yes
browseable = yes
EOF

sudo smbpasswd -a zrx        # 设 Samba 密码（独立于登录密码）
sudo testparm                # 语法检查，Loaded services file OK 即通过
sudo service smbd restart && sudo systemctl is-active smbd
```

Windows：右键"此电脑" → 映射网络驱动器 → 填 `\\<IP>\share` → 输 `zrx` + 密码。

> **坑**：heredoc 逐行粘贴会卡死；单行 printf 长命令会折行写坏配置。`testparm` 报 `Invalid octal number directory mask` = `directory mask = 0775` 被拆断，用 `sed` 合并成一行。

## 3. 环境工具链

**命令分块执行**——长命令粘贴折行后，后面包名全变独立命令装不上。

```bash
sudo apt update
sudo apt install -y binutils binutils-dev git git-lfs gnupg flex bison gperf build-essential zip curl zlib1g-dev unzip
sudo apt install -y gcc-multilib g++-multilib gcc-arm-none-eabi   # 交叉编译链 10.3
sudo apt install -y libc6-dev-i386 lib32ncurses6 lib32z1 ccache
sudo apt install -y libxml2-utils xsltproc bc gnutls-bin python3-pip ruby genext2fs device-tree-compiler libffi-dev pkg-config libssl-dev libelf-dev libdwarf-dev
sudo apt install -y u-boot-tools mtd-utils cpio doxygen openjdk-17-jre-headless texinfo mtools default-jre default-jdk
sudo apt install -y wget scons rsync git-core libxml2-dev xxd
sudo apt install -y libglib2.0-dev libpixman-1-dev kmod jfsutils reiserfsprogs xfsprogs squashfs-tools quota ppp vim locales libtinfo-dev
```

验证：`arm-none-eabi-gcc --version`（10.3）、`python3 --version`（≥3.8）、`java -version`（≥8）。

> **坑**：`gcc-arm-linux-gnueabi` 装不上是依赖冲突，MCU 用不到，跳过。文档要的 `python3.8`/`openjdk-8`/`lib32ncurses5-dev` 等 22.04 不存在，用 3.10/17/6 替代。

## 4. SDK 源码获取与解压

从南方电网数字研究院官方获取。厂商给的常是 **split 分卷**（`.aa`~`.ah` 各 2G）。

```bash
cd /home/zrx/share
file "PowerHarmony+_Release_V2.0.0.0.aa"   # gzip compressed data = 分卷
cat "PowerHarmony+_Release_V2.0.0.0".a[a-h] > PowerHarmony.tar.gz   # 合并 ~15G，需几分钟
tar -xzf PowerHarmony.tar.gz                 # 解出 Release_V2.0.0.0/
mv Release_V2.0.0.0 /home/zrx/               # 移到主目录（与 snap 同级）
ls /home/zrx                                  # 确认 snap 和 Release_V2.0.0.0 都在

# 确认移动成功后删原始包（省 ~30G）
cd /home/zrx/share
rm -f "PowerHarmony+_Release_V2.0.0.0".a[a-h] PowerHarmony.tar.gz
```

源码根目录 = `/home/zrx/Release_V2.0.0.0`

## 5. 安装 hb

```bash
cd /home/zrx/Release_V2.0.0.0
python3 -m pip uninstall ohos-build -y        # 卸旧版（pip 0.4.6 不认新结构）
python3 -m pip install ./build/hb             # 装 SDK 自带 hb
echo 'export PATH=~/.local/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

> **坑（py3.10 + prompt_toolkit）**：SDK 写死 `prompt_toolkit==1.0.14`。装 3.x → `hb set` 报 `cannot import name 'run_application'`；装 1.0.14 → 报 `cannot import name 'Mapping' from 'collections'`。
> 修复：装回 1.0.14，改 `prompt_toolkit/styles/from_dict.py` 一行 `from collections import Mapping` → `from collections.abc import Mapping`。**别再升级 prompt_toolkit**。新环境建议 Ubuntu 20.04（py3.8）免补丁。
> `ohos_config.json not found` 是 `hb set` 崩溃的连带报错，修好后自动生成。

## 6. 下载构建依赖（必须跑）

```bash
cd /home/zrx/Release_V2.0.0.0
./build/prebuilts_download.sh
```

不跑直接 `hb build` 报：`There is no gn executable file at .../prebuilts/build-tools/linux-x86/bin/gn`。执行中报错不影响编译。

## 7. 编译

```bash
cd /home/zrx/Release_V2.0.0.0
hb set           # 选 mini → 产品名
hb build -f      # build success 即成功；重编前可 rm -rf out/
```

产物：`out/<板卡>/<产品>/bin/`，含 `.bin` `.hex` `.elf`。

支持平台：

| SOC | 产品名 |
|---|---|
| AT32F403A | at32f4xx_product_demo |
| GD32F470 | gd32f4xx_product_demo |
| HC32F4A0 | hc32f4xx_product_demo |
| STM32L496 | stm32l4xx_product_demo |

## 8. 烧录（Windows）

1. 装厂商烧录工具（如 GD-link Programmer）+ USB 驱动
2. File→Open 打开 bin（填起始地址 `0x08000000`）或 hex
3. Target→Program 烧录
4. 板子 PA9/PA10 接串口，115200，复位看 log

## 后续路线

- **XTS 认证**：`hb build --gn-args build_xts=true`，改各平台串口/时钟/GPIO，加测试套 `ActsUtilsFileTest`、`ActsParameterTest`，搜 `failures` 共 36 个通过。
- **应用开发**：参考 SDK 文档第 10 章，改 `BUILD.gn`、`bundle.json`、`config.json` 后重编。
